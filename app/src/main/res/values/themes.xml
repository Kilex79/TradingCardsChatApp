<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Theme.SpaceCardsCollection" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimaryDark">@color/primary_color</item>
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorAccent">@color/white</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="popupTheme">@color/black</item>
        <item name="popupMenuStyle">@color/black</item>
        <item name="popupMenuBackground">@color/primary_color</item>
        <item name="popupWindowStyle">@color/black</item>
    </style>

    <style name="Theme.SpaceCardsCollection" parent="Base.Theme.SpaceCardsCollection" />

    <style name="Theme.SpaceCardsCollection.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>